{
  "name": "rate-limiter",
  "version": "1.0.0",
  "description": "Sliding Window rate limiter middleware — 5 req/min per IP using Redis",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "redis": "^4.6.12"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
